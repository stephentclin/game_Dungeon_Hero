# v8.3 Merge: `贴图改` + v8.2

This build uses v8.2 as the gameplay/UI base, then integrates the updated visual project:

- v8.2 real texture-button UI and settings menu remain the active UI layer.
- The uploaded commander platform/chief sprites and casting animation are active.
- The uploaded poison-pool artwork and animated terrain presentation are active.
- The uploaded unit summon visual effect is active.
- Updated project art assets were copied over the v8.2 equivalents where names overlap.
- v8.2 wall rebound, unit mechanics, hero seals, and upgrade/coin systems are preserved.

Please open this project as a fresh Godot project and run it once so Godot regenerates `.godot` imports.
